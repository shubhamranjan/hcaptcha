/**
 * Describes the parameters for the explicit rendering call.
 */
export interface IhCaptchaExplicitRenderParameters {
    container?: string | Element;
    theme?: 'light' | 'dark';
    size?: 'normal' | 'compact';
}
